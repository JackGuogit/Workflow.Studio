# Workflow.Studio V2 架构落地方案

> 状态：**已拍板**。本文汇总 2026-09-06 系列讨论的全部决策，作为 V2 重构的唯一设计基线。
> 范围：架构、契约、状态机、引擎、持久化、SDK 模板、CLI、里程碑。不含逐行代码实现。

---

## 1. 决策基线（决策记录）

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| D1 | 产品定位 | 向"类似 KNIME 的可视化任务/工作流平台"演进，非纯架构样板 |
| D2 | 数据形态 | 端口统一抽象；传递"任意注册的端口类型"，值类型注册表驱动 |
| D3 | 调度 | 事件驱动就绪队列（KNIME 风格），废弃波前分批 |
| D4 | 模型分层 | 图结构（Document）与运行时（Session）彻底分离 |
| D5 | 子工作流 | V1 引入元节点（Metanode）；组件/资产库后置 |
| D6 | 插件机制 | V1 程序集内特性自动发现（引用程序集扫描），外部目录加载后置 |
| D7 | 类型兼容 | 精确匹配：TypeId 相同即兼容；仅保留内置 `Any` 供目标端口通配 |
| D8 | 入线冲突 | 同一输入端口新连替换旧连 |
| D9 | 平台 | 仅 Windows：桌面 + CLI 均基于 `net10.0-windows`/Windows Desktop Runtime；不做 Linux |
| D10 | SDK 形态 | 节点逻辑与 WPF 视图同程序集（单工程单 DLL）；后续引入 property-tool 属性映射 UI，设置元数据现在定义 |
| D11 | 变量体系 | 弃用全局变量字典；采用 KNIME 流变量模型：容器作用域 + 类型化流变量 + 显式 in/out 映射 |
| D12 | 无头 CLI | V1 提供 `validate` 与 `run`（JSON 输出、退出码），支持 `--var` 覆盖入口变量 |
| D13 | 多文档 | 第一版支持多文档标签页；各文档独立 Session/日志/变量/历史；允许并行执行，设全局并发上限 |
| D14 | Configure | 做完整 configure/spec 级联（结构传播、绿/黄/红、dirty 追踪） |
| D15 | 失败语义 | 失败节点下游（含跨元节点传递依赖）进入 Blocked；独立分支继续执行 |
| D16 | 局部执行 | 提供"执行到选中节点"（向上游、clean 复用）与"从选中节点重跑下游"两个命令 |
| D17 | 元节点编辑 | 支持选中打包、边界端口/变量映射向导、面包屑下钻、拆开还原 |
| D18 | 路径基准 | 相对路径一律以"工作流文件所在目录"为基准，经运行时上下文注入节点 |
| D19 | 持久化 | v2 新格式（Document 纯结构）；不兼容 v1，无迁移器 |
| D20 | 断点/取消 | 会话级暂停门（多断点统一放行）；执行级取消命令 |

### 1.1 第二轮细化决策（2026-09-06 补充）

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| R1 | 元节点边界表示 | **方案 B：伪节点（Inlet/Outlet）**——边界是容器内的虚拟节点，所有连接统一为节点间边 |
| R2 | 设置引用变量机制 | **方案 A：settingsBindings**——设置属性可标记 `Bindable`，Document 存绑定表，引擎在 Configure/Execute 前解析并做类型检查 |
| R3 | 流变量声明的时点 | **方案 A：静态声明**——节点类型声明输出变量签名（名称 + TypeId），configure 阶段做冲突检查，执行期值必须匹配声明 |
| R4 | 执行中文档编辑 | **方案 A：执行期间锁定该文档**的结构/设置/变量编辑，其他文档不受影响 |
| R5 | 变量声明模型 | 每个容器（根或元节点）都有 `variableDeclarations`；根容器声明即"入口变量"，子容器声明来源为默认值或父级 in 映射 |
| R6 | 节点路径寻址 | 统一 `/<containerId>/<containerId>/<nodeId>` 格式，用于事件、断点、CLI `--up-to/--from`；节点 Id 仅在容器内唯一 |
| R7 | Spec 粒度 | 内置标量/文本类型用空 Spec；`image/frame` 可携带可预知结构（如按读模式的通道数）；Spec 变化触发下游重配 |
| R8 | 执行范围与缓存语义 | "执行全部" = 强制重跑全部并清空旧缓存；"执行到节点" = clean 复用；Reset 清空 Session 缓存并回到 NotConfigured |
| R9 | 调度约束 | Session 级异步调度器；worker 只执行节点任务，容器执行/等待不占用 worker，避免嵌套死锁 |
| R10 | 边界引用失效 | 删除/重映射被元节点边界引用的内部节点或端口时，先提示重新映射或禁止删除；重定向入线后目标及其下游按 dirty 级联 |

---

## 2. 目标架构总览

### 2.1 三大模型

```text
Catalog（注册表，进程级、只读）
├─ ValueTypeRegistry    端口值/流变量共用的类型注册表（TypeId 唯一）
├─ NodeTypeRegistry     节点类型（Factory 语义）
└─ PluginCatalog        能力插件目录（初始化/释放）

Document（文档模型，纯结构、可序列化、无运行时状态）
├─ WorkflowDocument       入口变量、节点、连接（根容器）
├─ ContainerNodeData      元节点：变量映射 + 嵌套 WorkflowDocument（内含边界伪节点）
├─ NodeData               结构：typeId、布局、设置、断点
└─ ConnectionData         边：source/dest node+port

Session（运行时模型，一次加载/一次执行的活体状态）
├─ WorkflowSession        文档 + 状态集合 + 调度器 + 作用域
├─ ContainerRuntime       容器级状态聚合 + 变量作用域
├─ NodeRuntime            状态机 + 输入/输出槽（Spec/Value）+ 断点
└─ ExecutionScope         声明变量 + 流变量（只读视图 + 传播）
```

约束：

- **Core 不引用 WPF**（引擎/契约/目录/持久化可在任何宿主运行）。
- Document 只被编辑器修改，Session 只被引擎修改；二者通过"附加（attach）"建立只读关联，不共享可变对象。
- 节点程序集（SDK）是 `net10.0-windows` 单 DLL：执行逻辑 + 设置 POCO + 可选 WPF 视图；执行路径不得触碰 WPF 类型。

### 2.2 程序集与依赖

```text
Workflow.Studio.Core            net10.0     领域契约、类型注册表、节点契约、引擎、
                                                     目录、持久化、Session 模型（无 UI）
Workflow.Studio.Nodes           net10.0-windows  内置节点：逻辑 + 设置 POCO + WPF 设置视图
Workflow.Studio.Plugins         net10.0-windows  内置能力插件（OpenCV 等）
Workflow.Studio.Desktop         net10.0-windows  WPF 桌面宿主（多文档标签、画布、面包屑）
Workflow.Studio.Cli             net10.0-windows  console 无头宿主（引用 Desktop Runtime，不开窗口）
Workflow.Studio.Theme/Nodify    net10.0-windows  视觉/封装（现状保留）
Workflow.Studio.Core.Tests      net10.0-windows  单元/引擎/持久化测试

依赖方向：
Core <── Nodes/Plugins
Core <── Desktop <── Nodes/Plugins
Core <── Cli <── Nodes/Plugins
```

> Core 保持 `net10.0` 不带 `-windows`：即使全平台方案是 Windows，也让引擎可被任何宿主加载，并天然防止 WPF 依赖渗入。

---

## 3. 类型注册与 SDK 契约

### 3.1 值类型注册表（端口与流变量共用）

```csharp
public sealed class ValueTypeDefinition
{
    string TypeId;          // 稳定字符串，持久化/兼容唯一依据，如 "text/plain"
    string DisplayName;
    Type PayloadType;       // 运行时 CLR 类型（端口值/变量值）
    bool CanBeFlowVariable; // 是否允许作为流变量（大对象类型关闭）
    bool IsOptional;        // 端口级默认可选性（节点声明时可覆盖）
    // 未来扩展：Spec 描述类型、值查看器/格式化器、序列化器
}
```

内置类型（Core 注册）：

| TypeId | Payload | 流变量 | 说明 |
| --- | --- | --- | --- |
| `text/plain` | string | 是 | 普通文本 |
| `text/csv` | string | 是 | CSV 文本 |
| `text/tsv` | string | 是 | TSV 文本 |
| `path/file` | string | 是 | 文件路径 |
| `image/frame` | WorkflowImageFrame | 否 | 编码图像帧（大对象） |
| `scalar/int64` | long | 是 | 整数变量/值 |
| `scalar/double` | double | 是 | 浮点 |
| `scalar/bool` | bool | 是 | 布尔 |
| `meta/any` | object | 否 | 仅允许出现在目标端口（通配接收方） |

兼容规则：源 TypeId == 目标 TypeId，或目标为 `meta/any`。不做子类型推断。

### 3.2 节点注册

```csharp
[WorkflowNodeType(
    TypeId = "demo.node.uppercase-transform",
    DisplayName = "转换节点",
    Category = "Transform")]
public sealed class UppercaseTransformNode : WorkflowNodeDefinition<UppercaseSettings>
{ ... }
```

发现规则（D6）：宿主启动时扫描**已引用程序集**中的特性：

- `[WorkflowNodeType]` → NodeTypeRegistry；
- `[WorkflowPlugin]` → PluginCatalog；
- `[ValueType]` → ValueTypeRegistry（第三方自定义端口类型）。

### 3.3 设置元数据（为 property-tool 预留）

```csharp
public sealed class UppercaseSettings : INodeSettings
{
    public string Title => "转换节点";
    public string Description => "...";

    [WorkflowSetting(DisplayName = "追加后缀", Editor = SettingEditorKind.Text)]
    public string Suffix { get; set; } = string.Empty;

    [WorkflowSetting(DisplayName = "输出目录", Editor = SettingEditorKind.Text, Bindable = true)]
    public string OutputDirectory { get; set; } = string.Empty;
}
```

- 设置类型为 POCO（不依赖 WPF，不继承 ObservableObject）；需要绑定的视图自行包装。
- 持久化按特性反射读写，不再对任意公共属性全量反射。
- 标记 `Bindable = true` 的设置字段可被容器声明变量绑定（R2）：绑定关系存于节点实例的 `settingsBindings: [{ setting, variable }]`；引擎在 Configure/Execute 前解析为字面值并做类型校验，节点代码读到的 `Settings` 已是解析后的值（无字符串替换语法）。
- 节点 Descriptor 携带 `SettingsViewTypeName`（字符串），Core/CLI 永不解析该类型；Desktop 经 ViewLocator 实例化。
- 节点类型静态声明输出变量签名（R3）：`OutputVariableDeclaration[]`（名称 + TypeId），configure 阶段即可做容器内同名冲突检查；执行期返回的变量必须匹配声明。

---

## 4. 领域模型：Document v2

### 4.1 结构

```text
WorkflowDocument
├─ SchemaVersion = 2
├─ VariableDeclarations[]  本容器变量声明（名称/TypeId/默认值；根容器即入口变量）
├─ Nodes[]               NodeDocument | ContainerNodeDocument
└─ Connections[]         ConnectionDocument

NodeDocument
├─ NodeId / NodeTypeId / Layout / IsBreakpointEnabled
├─ Settings              { typeId + 字段字典 }
└─ SettingsBindings      [{ setting, variable }]（可选，R2）

ContainerNodeDocument（元节点）
├─ 继承 NodeDocument 公共字段
├─ VariableMappings[]     in/out 变量映射（见第 7 章）
└─ InnerWorkflow          WorkflowDocument（递归）
```

### 4.2 连接

**所有连接都是"同一容器内两个节点之间的边"，元节点在外层也只是一个普通节点。** 边界通过元节点内工作流的伪节点表达（R1）：

- `core.boundary-in`（Inlet）：一个输出端口，NodeId 即元节点的外层输入端口 id；
- `core.boundary-out`（Outlet）：一个输入端口，NodeId 即元节点的外层输出端口 id；
- 伪节点端口的 TypeId/可选性由打包向导按被映射的内部端口确定；内层产生 `Inlet → 内部节点`、`内部节点 → Outlet` 的普通内边。
- 伪节点是 Core 内部通用类型，其唯一端口的 TypeId/名称**按实例持久化**（打包时由被映射端口决定）；普通节点的端口表面仍由节点类型契约派生、不落盘。元节点的外层端口表面 = 内层伪节点集合推导，外层端口 id 与伪节点 NodeId 一致、保持稳定。

```text
外层容器                           元节点 innerWorkflow
nodeA.out ──► meta.in-1             core.boundary-in("in-1").out ──► innerNode.in
                                     innerNode.out ──► core.boundary-out("out-1").in
nodeB.in  ◄── meta.out-1
```

- 打包向导：为每条跨边界边生成伪节点 + 内边；拆开还原反向操作并重连外层边。
- 删除/重映射被伪节点引用的内部节点或端口时，先提示重新映射或禁止删除（R10）。
- 同一输入端口只保留一条入线；新连替换旧连（D8）。连接不带类型字段（由两端端口声明推导）。

### 4.3 JSON 形状（示意）

```json
{
  "schemaVersion": 2,
  "variableDeclarations": [
    { "name": "seedText", "typeId": "text/plain", "defaultValue": "DevWorkflow Studio" }
  ],
  "nodes": [
    {
      "nodeId": "node-source",
      "nodeTypeId": "demo.node.text-source",
      "x": 80, "y": 120,
      "settings": { "suffix": "" }
    },
    {
      "nodeId": "node-meta",
      "nodeTypeId": "core.metanode",
      "x": 420, "y": 120,
      "settings": {},
      "variableMappings": [
        { "direction": "in", "source": "seedText", "target": "seed" },
        { "direction": "out", "source": "result", "target": "metaResult" }
      ],
      "innerWorkflow": {
        "variableDeclarations": [
          { "name": "seed", "typeId": "text/plain", "defaultValue": "" }
        ],
        "nodes": [
          { "nodeId": "in-1", "nodeTypeId": "core.boundary-in",
            "ports": [{ "portId": "out", "typeId": "text/plain" }] },
          { "nodeId": "inner-a", "nodeTypeId": "demo.node.uppercase-transform", "settings": {} },
          { "nodeId": "out-1", "nodeTypeId": "core.boundary-out",
            "ports": [{ "portId": "in", "typeId": "text/plain" }] }
        ],
        "connections": [
          { "sourceNodeId": "in-1", "sourcePortId": "out",
            "targetNodeId": "inner-a", "targetPortId": "incoming" },
          { "sourceNodeId": "inner-a", "sourcePortId": "result",
            "targetNodeId": "out-1", "targetPortId": "in" }
        ]
      }
    }
  ],
  "connections": [
    { "sourceNodeId": "node-source", "sourcePortId": "content",
      "targetNodeId": "node-transform", "targetPortId": "incoming" },
    { "sourceNodeId": "node-source", "sourcePortId": "content",
      "targetNodeId": "node-meta", "targetPortId": "in-1" }
  ]
}
```

> 说明：外层示例中 `node-meta.in-1` 的 id 与内层伪节点 `in-1` 一致，端口表面由伪节点推导；伪节点为 Core 内部类型，不面向 SDK 作者。

文件为单 JSON。无 v1 迁移。相对路径以文档所在目录为基准解析（D18）。变量声明（含绑定关系）随文档持久化；流变量与运行时值永不落盘。

---

## 5. 运行时 Session 与状态机

### 5.1 节点状态（NodeRuntimeState）

```text
NotConfigured ──Configure 成功──► Configured ──标记/就绪──► Queued ──► Running
     ▲                                │                        │
     │ 结构/设置/入口变量变更            │                        ▼
     └──────── 失效级联 ────────────────┘                     Paused（断点）→ Running
                                                               │
                                                               ▼
                                                          Succeeded（输出可见）
                                                               │
                                    失败 ──► Failed（红，输出清空）
                                    依赖失败 ──► Blocked（灰，不调度）
```

- `NotConfigured` 即"脏"；`Succeeded && !NotConfigured` 即"clean"，可复用输出。
- 状态迁移只由引擎/会话执行；节点模型不再有 Status。

### 5.2 端口槽（PortSlot）

```text
SpecSlot：Unknown → Configured（Configure 阶段传播）
ValueSlot：Empty → HasValue（节点 Succeeded 后可见，下游读取）
```

- 值只存于**上游输出槽**；下游入队时按连接**引用组装输入**（KNIME assembleInputData），不再向输入端口拷贝（保留现有 UI 预览：输入预览 = 读上游输出槽）。
- 非 Succeeded 节点输出不可见（输出门控）。
- Spec 内容按值类型定义（R7）：内置标量/文本为"空 Spec"（结构信息 = TypeId 本身）；`image/frame` 携带可预知结构（如读模式决定的通道数）。Spec 变化触发下游 NotConfigured 级联。

### 5.3 容器状态聚合

```text
有 Failed/Blocked           → 容器显示"有错误"
有 Running/Queued/Paused    → "执行中/暂停"
全部 Succeeded              → 绿
全部 Configured             → 黄（可执行）
含 NotConfigured            → 灰/黄（未配置）
```

事件载荷统一携带 `SessionId + ContainerPath + NodeId`，UI 按活动标签/面包屑过滤。

---

## 6. 引擎设计

### 6.1 执行流程

```text
1. Validate（结构/类型/变量/设置）
2. Configure 级联（可选，编辑后后台已做；执行前复查）
3. 按执行范围标记节点
4. 就绪队列调度（依赖完成计数）
5. 逐节点执行（worker 池限流）
6. 结果写回输出槽 + 流变量注册
7. 会话聚合状态发布
```

### 6.2 就绪队列

- 每个节点维护"未就绪前驱数"= 未完成且必需的输入连接数；归零即可入队。
- Session 内 worker 池上限默认 = 逻辑 CPU 核数（可配置）；全局并发上限（多文档共享）独立配置。
- 同文档内一次只允许一个执行命令；不同文档可并行（各自 Session）。

### 6.3 执行范围命令

1. **执行全部**：根容器整图执行。
2. **执行到选中节点**：沿依赖反向标记；遇到 `Succeeded && clean` 的前驱停止并复用其输出/流变量；目标在元节点内部时先保证外层链路执行到该元节点，再递归内部（跨层，D17）。
3. **从选中节点重跑下游**：强制重跑选中节点及其传递下游（同容器；跨元节点时递归容器边界）。

### 6.4 失败与 Blocked

- 节点失败：状态 → Failed，输出清空，错误信息入日志/历史。
- 其传递下游（含跨元节点边界）→ Blocked，不再调度。
- 独立分支不受影响，继续执行（D15）。
- 修复设置/结构后，受影响的失败节点及其下游自动回到 NotConfigured/Configured，可用命令 3 重跑。

### 6.5 取消与断点

- 执行级 CTS：取消后停止调度新节点，在途节点收到取消异常并优雅结束；已成功输出保留。
- 断点：会话级暂停门（单一信号 + 等待节点集合）；命中任意断点即暂停全部调度；Resume 统一放行。
- 命中事件携带完整节点路径；元节点内命中时 UI 自动下钻定位。

### 6.6 运行期约束（细化）

- **执行中文档锁（R4）**：某文档的 Session 执行期间，该文档的结构/设置/变量编辑命令全部禁用；其他文档不受影响，可继续编辑/执行。
- **节点路径寻址（R6）**：事件、断点、CLI 范围命令统一使用 `/<containerId>/<containerId>/<nodeId>`；节点 Id 仅在所在容器内唯一，跨容器允许重名。
- **异步调度（R9）**：调度器全异步；worker 只执行"节点任务"，容器启动/等待子图完成不占用 worker，避免嵌套容器叠加并发上限时死锁。
- **执行语义（R8）**："执行全部"强制重跑全部节点并清空旧缓存；"执行到选中节点"遇到 `Succeeded && clean` 前驱即复用；"从选中节点重跑下游"强制重跑选中节点及传递下游（含跨容器递归）；显式 Reset 清空 Session 缓存并回到 NotConfigured。
- **范围命令的容器寻址**：目标节点在元节点内时，`--up-to`/`--from` 一律按节点路径解析，跨层递归标记（D17）。

---

## 7. 变量体系（V2 核心）

### 7.1 概念

```text
容器声明变量（Declared） 每个容器一份 variableDeclarations：
                        根容器 = 入口变量（UI/CLI 可覆盖）
                        子容器 = 默认值 或 父级 in 映射
                        容器内只读
流变量（Flow）           节点按静态声明产出：名称 + TypeId + 值；沿容器内连接传播
```

规则：

- 变量与端口值共用 ValueTypeRegistry（TypeId 精确匹配，D11/V-B）。
- 节点可读：容器声明变量 + 自身前驱（传递闭包）产出的流变量；**兄弟分支不可见**。
- 节点类型静态声明输出变量（R3）；容器内所有产出（含元节点 out 映射的外名）同名冲突在 configure 阶段报错；执行期返回的变量名/类型必须与声明一致，否则该节点判失败。
- 仅 `CanBeFlowVariable = true` 的类型可用于变量（R4）。
- 设置绑定（V1 路线 A）：设置字段经 `settingsBindings` 绑定**容器声明变量**（configure 期已知，R2），引擎解析成字面值后交给 Configure/Execute；引用前驱流变量经 `request.Variables` 在执行逻辑内读取。设置表达式/两阶段 configure 为预留能力，不在 V1 实现。
- 执行期节点代码也可经 `request.Variables` 读取前驱流变量做逻辑决策，但不得写入（流变量只能产出、不可原地修改）。

### 7.2 元节点变量映射

- `VariableMappings` 存于 ContainerNodeDocument（不进入连接图，无变量连线）。
- `in` 映射：映射源 = 父容器中该元节点可见的变量（父声明变量或父级上游流变量）；容器开始时按父作用域变量快照复制为内部声明变量；内部修改不回传。
- `out` 映射：容器成功结束后，把内部流变量作为"该元节点产出的父级流变量"写回父作用域（外名在父容器做同名唯一校验）。
- 映射向导（打包时弹出）：方向 + 源/目标变量名 + TypeId 校验；类型不一致不可保存。
- 入口变量变更 → 依赖该变量的节点（静态追踪设置绑定）置 NotConfigured 并级联。

### 7.3 对现有代码的清理

- 删除 `WorkflowData.GlobalVariables`、`ExecutionContext.GlobalVariables`、`NodeExecutionResult.GlobalVariables`；
- 根文档改为 `VariableDeclarations`（即入口变量）；TextSource 种子文本改为入口变量 + 设置默认值；
- `LastTransform` 等演示变量改为流变量输出；"最近路径/最近预览"演示变量删除，改由日志/历史承载；
- UI 的 Global Variables 面板改为"入口变量 + 当前作用域变量查看器"（入口变量可编辑，流变量只读展示）。

---

## 8. 持久化 v2

- `SchemaVersion = 2`，单 JSON 文件；`load` 仅接受 v2，对未知版本给出明确错误（不迁移）。
- 保存内容 = 纯 Document：入口变量、节点（含设置元数据字典）、连接、元节点递归结构与映射、断点、布局。
- 不保存任何运行时状态（状态/值/历史/流变量）。
- 类型标识全部使用 TypeId/NodeTypeId（字符串）；`SettingsViewTypeName` 不落盘（由节点类型声明）。
- 加载后自动触发一次 Configure 级联，UI 呈现绿/黄/红，无需手动执行。

---

## 9. SDK 模板

模板工程（单 DLL，`net10.0-windows`）：

```text
MyWorkflowNode/
├─ MyNode.cs                执行逻辑（只依赖 Core，禁止 WPF using）
├─ MyNodeSettings.cs        设置 POCO + [WorkflowSetting] 元数据
├─ Views/MySettingsView.xaml  可选 WPF 视图（ViewLocator 按名称解析）
├─ MyPlugin.cs              可选能力插件
└─ HeadlessSmokeTest/       console 项目：引用本 DLL，执行一遍节点逻辑
```

- 模板内置 `HeadlessSmokeTest`（M1 纪律）：确保静态构造/模块初始化器不触碰 WPF。
- 无自定义视图的节点：桌面端由 property-tool（后续引入）按元数据渲染通用属性面板；现有节点视图继续有效。
- V1 只扫描宿主引用程序集，不加载外部目录；Catalog 接口按"外部清单加载"设计，SDK 开放时再实现目录扫描 + 隔离。

---

## 10. 无头 CLI

```text
ws validate <file>                      预检：结构/类型/变量/设置/Configure，输出 JSON 状态
ws run <file> [--var key=value]...     执行（可带执行范围命令）
    [--up-to <nodePath>]
    [--from <nodePath>]
```

- `nodePath` 使用容器路径寻址（R6）：`/containerA/nodeId` 或 `/nodeId`；根容器节点可省略前导容器段。
- `--var` 只覆盖根容器入口变量；未知变量名或 TypeId 类型不匹配按参数错误处理。
- 输出 JSON：节点状态（含 Blocked）、日志、错误、流变量摘要、执行历史；退出码 0=成功 / 1=业务失败（有 Failed/Blocked 或校验失败）/ 2=参数或文件错误。
- 运行环境：Windows Desktop Runtime（console 宿主，不创建窗口/UI 线程）。
- console 项目自身为 `net10.0-windows`，不需要 `UseWPF`；目标机必须安装 Windows Desktop Runtime（否则含 WPF 引用的节点程序集无法加载）。
- 相对路径按文档目录解析；`--var` 覆盖入口变量。

---

## 11. 里程碑与验收

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M0 | Catalog 基建：ValueType/NodeType/Plugin 特性 + 注册表 + 发现 | 内置类型与 10 节点全部经特性注册；注册表单测通过 |
| M1 | Document v2：纯结构模型 + 元节点嵌套 + JSON 读写 | 保存/加载往返含嵌套与变量映射；无 v1 兼容 |
| M2 | Session 化：NodeRuntime/PortSlot/状态机 + Configure 级联 + 脏状态 | 编辑结构/设置后下游自动变黄；输出门控；状态事件带路径 |
| M3 | 就绪队列引擎：范围命令、失败/Blocked、clean 复用、取消、断点暂停门 | 并行性、执行到节点、下游重跑、失败分支语义均有测试 |
| M4 | 变量体系：入口/声明/流变量 + 传播 + 映射 + 移除 GlobalVariables | 变量类型校验、名称唯一、兄弟不可见、in/out 映射测试通过 |
| M5 | 元节点：打包向导、边界端口/变量映射、面包屑下钻、拆开还原、跨层执行 | 端到端嵌套工作流执行 + 打包/拆开往返测试 |
| M6 | 多文档 UI：标签页、独立 Session/日志/变量/历史、全局并发上限 | 双文档并行执行与独立调试互不干扰 |
| M7 | CLI：validate/run/--var/JSON/退出码 | Windows Desktop Runtime 上 headless 全流程测试 |
| M8 | SDK 模板 + HeadlessSmokeTest + 文档 | 新模板节点可在桌面与 CLI 双宿主运行；property-tool 接口预留 |

每里程碑交付物含：代码、测试、文档更新；M0–M3 建议单 PR 落地，M4/M5 为高风险核心可拆分评审。

---

## 12. 明确不做（本期范围外）

- Linux/Server Core 运行（D9）；保留 Core 无 UI 边界，未来可拆分平台变体。
- KNIME `.knwf` 兼容（不做）。
- 组件/资产库复用（元节点已支持；组件化后置）。
- 循环、Try-Catch 作用域、远程执行（KNIME 高级控制流，均后置）。
- 设置表达式引用流变量（路线 B）、变量端口连线（显式变量边）。
- 磁盘 spill/大数据表抽象。
- 外部插件目录动态加载（SDK 开放时再做）。
