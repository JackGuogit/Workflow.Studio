<UserControl
    x:Class="Workflow.Studio.Nodes.OpenCvThreshold.View.OpenCvThresholdNodeSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="15"
            FontWeight="SemiBold"
            Text="阈值参数" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            Foreground="#6B7280"
            Text="如输入为彩色图，可启用自动灰度化后再执行阈值处理。"
            TextWrapping="Wrap" />

        <CheckBox
            Grid.Row="2"
            Margin="0,12,0,0"
            Content="自动转换为灰度图"
            IsChecked="{Binding AutoConvertToGrayscale, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="3"
            Margin="0,16,0,0"
            FontWeight="SemiBold"
            Text="Threshold" />

        <TextBox
            Grid.Row="4"
            Margin="0,8,0,0"
            Padding="10"
            Text="{Binding ThresholdValue, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="5"
            Margin="0,16,0,0"
            FontWeight="SemiBold"
            Text="Max Value / Mode" />

        <Grid Grid.Row="6" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                Padding="10"
                Text="{Binding MaxValue, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox
                Grid.Column="2"
                Padding="10,6"
                ItemsSource="{Binding AvailableThresholdModes}"
                SelectedItem="{Binding ThresholdMode, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
    </Grid>
</UserControl>
