<UserControl
    x:Class="Workflow.Studio.Nodes.TextSource.View.TextSourceNodeSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="220"
    d:DesignWidth="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="15"
            FontWeight="SemiBold"
            Text="输出文本" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            Foreground="#6B7280"
            Text="为空时将回退到全局变量 GlobalVariablesSeedText。"
            TextWrapping="Wrap" />

        <TextBox
            Grid.Row="2"
            MinHeight="120"
            Margin="0,12,0,0"
            Padding="10"
            AcceptsReturn="True"
            Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
