<UserControl
    x:Class="Workflow.Studio.Nodes.OpenCvImageSave.View.OpenCvImageSaveNodeSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="180"
    d:DesignWidth="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="15"
            FontWeight="SemiBold"
            Text="图片输出路径" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            Foreground="#6B7280"
            Text="若目录不存在会自动创建，建议使用 .png 或 .jpg 扩展名。"
            TextWrapping="Wrap" />

        <TextBox
            Grid.Row="2"
            Margin="0,12,0,0"
            Padding="10"
            Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
