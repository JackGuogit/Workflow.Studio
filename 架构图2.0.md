# Workflow.Studio 架构图 2.0

> 本文档基于 `p:\CSharp\WPF\DevWorkflow.Studio\Workflow.Studio` 解决方案**实际代码**逐层梳理而成，
> 与早期《总体架构.md》《工作流架构图.md》的设想稿不同，本文全部内容以当前源码实现为准。

---

## 目录

- [1. 总体架构视图](#1-总体架构视图)
- [2. 解决方案与依赖关系](#2-解决方案与依赖关系)
- [3. 应用启动与依赖注入](#3-应用启动与依赖注入)
- [4. Core 层（领域模型与执行引擎）](#4-core-层领域模型与执行引擎)
  - [4.1 领域模型（Models）](#41-领域模型models)
  - [4.2 类型兼容规则（PortTypeCompatibility）](#42-类型兼容规则porttypecompatibility)
  - [4.3 节点抽象（IWorkflowNode）](#43-节点抽象iworkflownode)
  - [4.4 插件契约（IWorkflowPlugin）](#44-插件契约iworkflowplugin)
  - [4.5 执行上下文（ExecutionContext）](#45-执行上下文executioncontext)
  - [4.6 执行引擎（WorkflowEngine）](#46-执行引擎workflowengine)
  - [4.7 节点注册中心（NodeManager / NodeFactory）](#47-节点注册中心nodemanager--nodefactory)
  - [4.8 事件总线（WorkflowEventHub）](#48-事件总线workfloweventhub)
  - [4.9 连接校验器（WorkflowConnectionValidator）](#49-连接校验器workflowconnectionvalidator)
  - [4.10 插件管理器（PluginManager）](#410-插件管理器pluginmanager)
  - [4.11 调试控制器（WorkflowDebugController）](#411-调试控制器workflowdebugcontroller)
  - [4.12 持久化服务（WorkflowPersistenceService）](#412-持久化服务workflowpersistenceservice)
- [5. Workbench 层（工作台 UI）](#5-workbench-层工作台-ui)
  - [5.1 ViewModel 体系](#51-viewmodel-体系)
  - [5.2 View 与交互](#52-view-与交互)
- [6. Desktop 层（桌面宿主）](#6-desktop-层桌面宿主)
- [7. Nodes 层（内置节点）](#7-nodes-层内置节点)
- [8. Plugins 层（内置插件）](#8-plugins-层内置插件)
- [9. Theme 层（主题系统）](#9-theme-层主题系统)
- [10. Nodify 封装层](#10-nodify-封装层)
- [11. 测试项目](#11-测试项目)
- [12. 核心流程时序](#12-核心流程时序)
- [13. 数据流与事件流](#13-数据流与事件流)
- [14. 设计模式与架构特性](#14-设计模式与架构特性)
- [15. 已知问题与演进方向](#15-已知问题与演进方向)

---

## 1. 总体架构视图

```text
+------------------------------------------------------------------------------------------+
|                        Presentation Layer（WPF 视图）                                      |
|  Workflow.Studio.Desktop                                                                  |
|   └─ MainWindow / MainView                      （壳层窗口）                              |
|  Workflow.Studio.Workbench / Views                                                       |
|   └─ WorkflowWorkbenchView（NodifyEditor 画布、节点库、执行日志、Global Variables 面板）    |
|   └─ NodeParameterEditorWindow（节点参数编辑窗口）                                          |
+------------------------------------------------------------------------------------------+
|                     ViewModel / 应用逻辑层（CommunityToolkit.Mvvm）                        |
|  Workflow.Studio.Workbench / ViewModels                                                   |
|   └─ WorkflowWorkbenchViewModel（工作台中枢：命令、集合、图状态、事件订阅）                  |
|   └─ NodeViewModel / PortViewModel / PortGroupViewModel                                  |
|   └─ ConnectionViewModel / PendingConnectionViewModel / NodeLibraryItemViewModel         |
|   └─ ExecutionLogItemViewModel / NodeParameterEditorViewModel                            |
+------------------------------------------------------------------------------------------+
|                        Application / Domain 层（执行引擎与领域模型）                       |
|  Workflow.Studio.Core                                                                     |
|   ├─ Services：WorkflowEngine、NodeManager、NodeFactory、PluginManager、                 |
|   │             WorkflowEventHub、WorkflowConnectionValidator、                          |
|   │             WorkflowDebugController、WorkflowPersistenceService                       |
|   ├─ Runtime：ExecutionContext（执行历史/快照/全局变量/端口快照）                          |
|   ├─ Nodes：IWorkflowNodeDefinition、NodeDescriptor、WorkflowNodeDefinition<TSettings>    |
|   ├─ Plugins：IWorkflowPlugin、能力接口（ITextTransformPlugin/IPreviewPlugin/OpenCV…）     |
|   └─ Models：WorkflowData/NodeData/PortData/ConnectionData/PortMetadata/…                |
+------------------------------------------------------------------------------------------+
|                   插件实现 / 节点实现（可扩展能力）                                         |
|  Workflow.Studio.Plugins：UppercaseTransformPlugin、PreviewPlugin、                      |
|                           OpenCvImageCodecPlugin、OpenCvImageProcessingPlugin             |
|  Workflow.Studio.Nodes：10 个内置节点 + 节点设置 VM + 设置视图 XAML                        |
+------------------------------------------------------------------------------------------+
|             基础支撑层（视觉与第三方控件适配）                                              |
|  Workflow.Studio.Nodify：包装 Nodify 7.3.0 的面容（Facade）控件 + 交互扩展                |
|  Workflow.Studio.Theme：Light/Dark 主题资源 + WorkbenchThemeManager + Nodify 样式桥接      |
+------------------------------------------------------------------------------------------+
```

分层原则（自下而上依赖）：

- **Core** 不依赖任何其他项目，纯领域与引擎；
- **Nodes / Plugins** 只依赖 Core（能力接口由 Core 定义，实现由 Plugins 提供，节点通过构造注入消费能力）；
- **Workbench** 依赖 Core + Theme + Nodify（不直接引用第三方 Nodify 命名空间，遵循 `.trae/rules/nodify.md` 封装约定）；
- **Desktop** 是唯一可执行程序集，组合所有下层项目，并通过 Autofac 完成装配；
- **Theme** 依赖 Nodify 封装层，负责把第三方默认样式桥接为 `WorkflowWorkbench.*` 稳定资源键。

---

## 2. 解决方案与依赖关系

解决方案文件：`Workflow.Studio.slnx`（.NET 10 SDK 风格，XML 单文件方案格式）。

| 项目 | 目标框架 | 项目引用 | NuGet 包 | 职责 |
| --- | --- | --- | --- | --- |
| `Workflow.Studio.Core` | net10.0 | — | — | 领域模型、节点/插件契约、执行引擎、调试、持久化 |
| `Workflow.Studio.Core.Tests` | net10.0-windows | Core, Nodes, Plugins | xunit 2.9.2, Microsoft.NET.Test.Sdk 17.14.1 | 引擎/校验/持久化/OpenCV 节点单元测试 |
| `Workflow.Studio.Nodify` | net10.0-windows (WPF) | — | Nodify 7.3.0 | 第三方节点编辑器封装层 |
| `Workflow.Studio.Theme` | net10.0-windows (WPF) | Nodify | — | 主题资源 + Nodify 样式桥接 + 主题管理 |
| `Workflow.Studio.Workbench` | net10.0-windows (WPF) | Core, Nodify, Theme | CommunityToolkit.Mvvm 8.4.2 | 工作台 UI 与交互 |
| `Workflow.Studio.Nodes` | net10.0-windows (WPF) | Core | CommunityToolkit.Mvvm 8.4.2, OpenCvSharp4 4.10.0 | 内置节点与设置视图 |
| `Workflow.Studio.Plugins` | net10.0 | Core | OpenCvSharp4 4.10.0 | 内置能力插件 |
| `Workflow.Studio.Desktop` | net10.0-windows (WPF, WinExe) | Core, Nodes, Plugins, Workbench | Autofac 9.1.0, CommunityToolkit.Mvvm 8.4.2 | 启动宿主 + DI 装配 |

依赖图（箭头表示“引用”）：

```text
                Workflow.Studio.Desktop (启动项)
                │  ├──────► Core
                │  ├──────► Nodes ──────► Core
                │  ├──────► Plugins ─────► Core
                │  └──────► Workbench ───► Core
                │                ├──────► Theme ──────► Nodify ────► (Nodify 7.3.0 NuGet)
                │                └──────► Nodify
   Workflow.Studio.Core.Tests
        ├──► Core
        ├──► Nodes
        └──► Plugins
```

> 说明：`Workflow.Studio.Desktop/Views/MainView.xaml` 存在但未在启动流程中使用（当前 `App` 直接
> `Resolve<MainWindow>`），属于历史遗留的可选壳层视图。`Workflow.Studio.Workbench/Class1.cs` 为占位文件。

---

## 3. 应用启动与依赖注入

### 3.1 App（[App.xaml.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/App.xaml.cs)）

```text
OnStartup
 ├─ BuildContainer()           构建 Autofac IContainer
 │    └─ builder.RegisterModule<ApplicationModule>()
 ├─ Resolve<MainWindow>()      解析主窗口
 └─ mainWindow.Show()
OnExit
 └─ _container?.Dispose()      释放容器（含插件 Dispose）
```

- App.xaml 在应用级资源中合并主题字典：`Icons.xaml` → `WorkflowWorkbench.xaml` → `Light.xaml`。
- 主题字典采用**静态合并 + 动态切换**双机制：启动时静态合并，运行中由 `WorkbenchThemeManager` 动态替换主题字典。

### 3.2 ApplicationModule（[ApplicationModule.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/ApplicationModule.cs)）

注册内容一览（均为 `SingleInstance` 单例）：

| 注册项 | 类型/接口 | 说明 |
| --- | --- | --- |
| 插件 | `UppercaseTransformPlugin` → `IWorkflowPlugin` + `ITextTransformPlugin` | 文本大写转换能力 |
| 插件 | `PreviewPlugin` → `IWorkflowPlugin` + `IPreviewPlugin` | 预览生成能力 |
| 插件 | `OpenCvImageCodecPlugin` → `IWorkflowPlugin` + `IOpenCvImageCodecPlugin` | 图片读写能力 |
| 插件 | `OpenCvImageProcessingPlugin` → `IWorkflowPlugin` + `IOpenCvImageProcessingPlugin` | 灰度/二值化能力 |
| 服务 | `PluginManager`（工厂注册） | 收集所有 `IWorkflowPlugin` 注册，并在解析时同步 `InitializeAsync` |
| 服务 | `NodeManager`（工厂注册） | 注册 10 个节点类型，节点构造时注入能力插件 |
| 服务 | `WorkflowEventHub` / `WorkflowEngine` / `NodeFactory` | 事件总线、执行引擎、节点工厂 |
| 服务 | `WorkflowConnectionValidator` → `IWorkflowConnectionValidator` | 连接校验 |
| 服务 | `WorkflowDebugController` → `IWorkflowDebugController` | 调试控制 |
| 服务 | `WorkflowPersistenceService` → `IWorkflowPersistenceService` | 持久化 |
| 服务 | `WorkflowDocumentPickerService` → `IWorkflowDocumentPickerService` | 文件对话框 |
| VM/View | `WorkflowWorkbenchViewModel` / `MainWindowViewModel` / `MainWindow` | 工作台 VM、壳层 VM、主窗口 |

注册的 10 个节点类型：

```text
demo.node.text-source          输入节点（Source）
demo.node.csv-read             CSV读取节点（Source）
demo.node.uppercase-transform  转换节点（Transform）
demo.node.csv-to-tsv-transform CSV转TSV节点（Transform）
demo.node.preview              预览节点（Output）
demo.node.tsv-save             TSV保存节点（Output）
opencv.node.image-read         图片读取节点（OpenCV）
opencv.node.grayscale          灰度化节点（OpenCV）
opencv.node.threshold          二值化节点（OpenCV）
opencv.node.image-save         图片保存节点（OpenCV）
```

---

## 4. Core 层（领域模型与执行引擎）

### 4.1 领域模型（Models）

文件：[WorkflowModels.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Models/WorkflowModels.cs)

#### 枚举

| 枚举 | 值 | 语义 |
| --- | --- | --- |
| `PortDirection` | `Input` / `Output` | 端口方向（输入/输出） |
| `PortStatus` | `Ready` / `Connected` / `HasData` / `Failed` | 端口运行状态 |
| `NodeStatus` | `Ready` / `Paused` / `Running` / `Success` / `Failed` | 节点运行状态（Paused 由断点触发） |

#### 接口

| 接口 | 成员 | 说明 |
| --- | --- | --- |
| `IPort` | `Metadata`(PortMetadata)、`Direction`、`Value`(object?)、`Status`(PortStatus) | 端口只读契约 |
| `INode` | `Metadata`(NodeMetadata)、`NodeTypeId`、`InputPorts`、`OutputPorts` | 节点只读契约 |
| `INodeSettings` | `Title`、`Description` | 节点设置契约（用于持久化排除与编辑窗口描述） |

#### PortMetadata（端口元数据，init-only 不可变）

| 字段/属性 | 类型 | 用途 |
| --- | --- | --- |
| `Id` | string | 端口唯一标识（节点内） |
| `Name` | string | 显示名称 |
| `DataType` | Type | CLR 数据类型（连接兼容性依据之一） |
| `SemanticTypeKey` | string | 语义类型键（MIME 风格，见 4.2），默认 `""` |
| `GroupName` | string | 端口分组名，默认 `"Default"`（用于 UI 折叠分组） |
| `Description` | string | 描述文本 |

#### PortData（端口实例，可变）

| 字段/属性 | 类型 | 说明 |
| --- | --- | --- |
| `Metadata` / `Direction` | — | 构造传入，只读 |
| `Value` | object? | 端口当前值（私有 set） |
| `Status` | PortStatus | 默认 `Ready`（私有 set） |
| `IsCollapsed` | bool | UI 折叠状态 |

关键方法：

- `SetValue(object? value, string? context)`：先经 `PortTypeCompatibility.EnsureValueMatches` 做运行时类型校验（校验失败抛 `InvalidOperationException`），再赋值；值为 null 时置状态 `Connected`，非 null 置 `HasData`。
- `MarkConnected()`：仅当 `Ready` 时升级为 `Connected`（保留 `HasData` 优先级）。
- `MarkFailed()` / `Clear()` / `SetCollapsed(bool)`：置失败、清空值并复位 `Ready`、设置折叠。

#### NodeMetadata（节点元数据）

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `Id` | string | 全局节点标识（Guid 风格字符串，跨工作流唯一） |
| `Name` / `Category` / `Description` | string | 显示名 / 分类 / 描述 |

#### NodeData（节点实例，实现 INode）

| 字段/属性 | 类型 | 说明 |
| --- | --- | --- |
| `Metadata` / `NodeTypeId` | — | 构造传入 |
| `Status` | NodeStatus | 默认 `Ready` |
| `IsBreakpointEnabled` | bool | 断点标记（引擎执行前检查） |
| `LastMessage` / `LastErrorMessage` | string? | 最近执行消息 / 错误信息（UI 高亮用） |
| `Layout` | NodeLayoutData | 画布坐标（`X`/`Y`） |
| `Settings` | INodeSettings? | 节点参数对象（私有 set） |
| `SettingsViewType` | Type? | 参数编辑视图类型（反射创建） |
| `InputPorts` / `OutputPorts` | Collection\<PortData> | 输入/输出端口集合 |
| `HasSettings` | bool | 是否有设置对象 |

关键方法：

- `AddInputPort(id, name, dataType, groupName="Input", description, semanticTypeKey)`：构造 `PortMetadata` + `PortData(Input)` 加入 `InputPorts`，返回端口。
- `AddOutputPort(...)`：同输入，groupName 默认 `"Output"`。
- `FindPort(string portId)`：在输入+输出端口中按 Id 忽略大小写查找。
- `GetSettings<TSettings>()`：强类型获取设置，类型不符抛异常。
- `SetSettings<TSettings>(settings, settingsViewType)`：注入设置与对应视图类型。
- `SetStatus` / `SetBreakpointEnabled` / `SetLastMessage` / `SetLastError` / `ClearRuntimeState`：运行状态修改入口（`ClearRuntimeState` 清空消息并把状态复位为 `Ready`）。

#### ConnectionData（连接，init-only 不可变）

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `SourceNodeId` / `SourcePortId` | string | 源节点 + 源输出端口 |
| `TargetNodeId` / `TargetPortId` | string | 目标节点 + 目标输入端口 |

#### WorkflowData（工作流图根对象）

| 字段/属性 | 类型 | 说明 |
| --- | --- | --- |
| `Nodes` | Collection\<NodeData> | 节点集合 |
| `Connections` | Collection\<ConnectionData> | 连接集合 |
| `GlobalVariables` | IDictionary\<string, object?> | 全局变量（`StringComparer.OrdinalIgnoreCase`，跨节点共享） |

方法：`AddNode(node)` 加入并返回；`Connect(s4, p4, t4, p4)` 构造连接并加入。

#### 执行 DTO

| 类型 | 字段 | 说明 |
| --- | --- | --- |
| `NodeExecutionRequest` | `Node`、`InputValues`(IReadOnlyDictionary\<string,object?>) | 节点执行输入（InputSnapshot 传入） |
| `NodeExecutionResult` | `OutputValues`(按端口 Id 索引)、`GlobalVariables`、`Message` | 节点执行输出，OutputValues/GlobalVariables 字典均为大小写不敏感 |
| `NodeExecutionRecord` | `NodeId/NodeName/StartedAt/FinishedAt/Status/Message/InputSnapshot/OutputSnapshot` | 单次节点执行历史记录 |

#### 其它模型

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `WorkflowDebugModels.cs` | `WorkflowLogLevel`(Debug/Info/Warning/Error)、`WorkflowLogEntry`(Timestamp/Level/Message/NodeId/NodeName) | 调试日志模型 |
| `WorkflowImageFrame.cs` | `WorkflowImageFrame`：`Content`(byte[] 编码后像素)、`Format`(".png" 默认)、`Width`/`Height`/`Channels`/`IsGrayscale`/`SourcePath` | OpenCV 图像在端口间传递的载体（字节流，避免 Mat 生命周期问题） |
| `WorkflowImageReadMode.cs` | `Unchanged`/`Color`/`Grayscale` | 图片读取模式 |
| `WorkflowThresholdMode.cs` | `Binary`/`BinaryInverted`/`Truncate`/`ToZero`/`ToZeroInverted` | 二值化模式 |
| `WorkflowPortSemanticTypes.cs` | 常量：`PlainText`="text/plain"、`CsvText`="text/csv"、`TsvText`="text/tab-separated-values"、`PreviewText`="text/preview"、`FilePath`="path/file"、`ImageFrame`="image/frame" | 语义类型键定义（MIME 风格） |

### 4.2 类型兼容规则（PortTypeCompatibility）

文件：[PortTypeCompatibility.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Models/PortTypeCompatibility.cs)

纯静态工具类，双层兼容判定：

| 方法 | 逻辑 |
| --- | --- |
| `AreCompatible(PortMetadata, PortMetadata)` | CLR 类型兼容 **且** 语义类型兼容 |
| `AreCompatible(Type, Type)` | `NormalizeNullableType`（剥离 `Nullable<>`）后做 `IsAssignableFrom` 判断（协变兼容） |
| `IsValueCompatible(Type, object?)` | null 恒兼容；否则 `NormalizeNullableType(declaredType).IsInstanceOfType(value)` |
| `EnsureValueMatches(Type, object?, context)` | 不兼容时抛出带中文上下文的中文异常 |
| `AreSemanticTypesCompatible(string?, string?)` | 两端均为空 → 兼容；否则忽略大小写比较 Trim 后键 |
| `GetDisplayName(Type)` / `GetSemanticDisplayName(string?)` | 显示名 / 语义名（空时显示"未指定"） |

> 校验语义：`text/csv` 与 `text/plain` 虽然都是 `string` 类型，但因语义键不同会被判不兼容——这就是
> `WorkflowConnectionValidatorTests` 中 CSV→TSV 被拒绝的原因。

### 4.3 节点抽象（IWorkflowNode）

文件：[IWorkflowNode.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Nodes/IWorkflowNode.cs)

#### IWorkflowNodeDefinition

```csharp
public interface IWorkflowNodeDefinition
{
    NodeDescriptor Descriptor { get; }
    NodeData CreateNode(string nodeId, double x = 0, double y = 0);
    Task<NodeExecutionResult> ExecuteAsync(NodeExecutionRequest request,
        CoreExecutionContext executionContext, CancellationToken cancellationToken);
}
```

`NodeDescriptor`：`NodeTypeId`(注册键)、`DisplayName`、`Category`、`Description`。

#### WorkflowNodeDefinition<TSettings>（抽象基类，模板方法模式）

`CreateNode(nodeId, x, y)` 是模板骨架，固定流程：

```text
CreateSettingsCore()               ① 创建设置对象（子类实现）
new NodeData(metadata, typeId)     ② 由 Descriptor 构建元数据
node.Layout.X/Y = x/y              ③ 布局坐标
node.SetSettings(settings, GetSettingsViewType())  ④ 绑定设置 + 设置视图类型
BuildNode(node, settings)          ⑤ 定义端口（子类实现）
```

`ExecuteAsync`（非泛型）将请求转发到泛型重载，并从 `request.Node.GetSettings<TSettings>()` 取出强类型设置。

子类需实现的三个抽象成员：

| 成员 | 职责 |
| --- | --- |
| `Descriptor` | 声明节点身份与分类 |
| `CreateSettingsCore()` | 创建默认设置实例 |
| `BuildNode(NodeData, TSettings)` | 调用 `AddInputPort`/`AddOutputPort` 定义端口 |
| `ExecuteAsync(request, context, settings, ct)` | 实现执行逻辑并返回 `NodeExecutionResult` |

可覆写成员：`GetSettingsViewType()` 默认返回 null（无参数编辑视图）。

### 4.4 插件契约（IWorkflowPlugin）

文件：[IWorkflowPlugin.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Plugins/IWorkflowPlugin.cs) / [IOpenCvImagePlugins.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Plugins/IOpenCvImagePlugins.cs)

```text
IWorkflowPlugin : IAsyncDisposable
 ├─ PluginMetadata Metadata          元数据（Id/Name/Description/Version/Publisher/Capabilities）
 └─ ValueTask InitializeAsync(PluginInitializationContext, CancellationToken)

能力接口（面向节点的可复用能力契约）：
 ├─ ITextTransformPlugin            ValueTask<string> TransformToUppercaseAsync(input, ct)
 ├─ IPreviewPlugin                  ValueTask<string> BuildPreviewAsync(input, ct)
 ├─ IOpenCvImageCodecPlugin         ValueTask<WorkflowImageFrame> LoadAsync(path, mode, ct)
 │                                  ValueTask SaveAsync(image, path, ct)
 └─ IOpenCvImageProcessingPlugin    ValueTask<WorkflowImageFrame> ConvertToGrayscaleAsync(image, ct)
                                    ValueTask<WorkflowImageFrame> ApplyThresholdAsync(image, threshold,
                                        maxValue, mode, autoConvertToGrayscale, ct)
```

设计要点：**节点依赖能力接口，不依赖具体插件类**；`PluginInitializationContext` 目前为空标记类（预留扩展点）。

### 4.5 执行上下文（ExecutionContext）

文件：[ExecutionContext.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Runtime/ExecutionContext.cs)

私有数据字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `_syncRoot` | object | 历史/快照列表访问锁 |
| `_history` | List\<NodeExecutionRecord> | 节点执行历史 |
| `_snapshots` | List\<NodeExecutionSnapshot> | 节点执行快照（含全局变量副本） |
| `_globalVariables` | ConcurrentDictionary\<string, object?> | 全局变量（大小写不敏感） |
| `_portSnapshots` | ConcurrentDictionary\<string, object?> | 端口值快照，键为 `nodeId:portId` |

公开属性：

| 属性 | 类型 | 说明 |
| --- | --- | --- |
| `History` | IReadOnlyList\<NodeExecutionRecord> | 锁内 `ToList()` 快照返回 |
| `Snapshots` | IReadOnlyList\<NodeExecutionSnapshot> | 锁内快照返回 |
| `PortSnapshots` | IReadOnlyDictionary | 端口快照只读视图 |
| `GlobalVariables` | IDictionary\<string, object?> | 引擎可读写 |

公开方法：

- `CapturePortValue(nodeId, portId, value)`：写 `_portSnapshots["nodeId:portId"]`。
- `AddRecord(record)`：锁内追加历史。
- `CaptureNodeSnapshot(nodeId, status, inputSnapshot, outputSnapshot)`：将当前全局变量复制一份连同输入输出快照打包成 `NodeExecutionSnapshot` 追加。
- `GetPortSnapshot(nodeId, portId)`：读取端口快照。

`NodeExecutionSnapshot` 字段：`NodeId`、`CapturedAt`(UtcNow)、`Status`、`InputSnapshot`、`OutputSnapshot`、`GlobalVariables`（均为快照副本，避免共享引用）。

### 4.6 执行引擎（WorkflowEngine）

文件：[WorkflowEngine.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/WorkflowEngine.cs)

依赖注入：`PluginManager`、`NodeManager`、`WorkflowEventHub`、`IWorkflowConnectionValidator`、`IWorkflowDebugController`。

对外事件：

- `event EventHandler<NodeStatusChangedEventArgs> NodeStatusChanged`
- `event EventHandler<PortValueChangedEventArgs> PortValueChanged`

`NodeStatusChangedEventArgs`：`NodeId`、`Status`；`PortValueChangedEventArgs`：`NodeId`、`PortId`、`Value`、`Status`。

#### ExecuteAsync（执行主入口）

```text
await _executionGate.WaitAsync(0)    ① 非阻塞获取执行门（同时只允许一次执行，否则抛异常）
_nodeManager.AttachWorkflow(workflow) ② 建立节点索引
_connectionValidator.EnsureWorkflowIsValid(workflow) ③ 全图校验
ResetWorkflowState(workflow)         ④ 清空节点/端口运行态，并按连接 MarkConnected
_debugController.StartSession()      ⑤ 调试会话开始
await _pluginManager.InitializeAsync() ⑥ 插件幂等初始化
new ExecutionContext(globalVariables) ⑦ 执行上下文
BuildExecutionBatches(workflow)      ⑧ 拓扑排序生成批次
for 每批: Task.WhenAll(batch.Select(ExecuteNodeAsync)) ⑨ 逐批并行执行
SyncGlobalVariables(workflow, context) ⑩ 把上下文全局变量回写 WorkflowData
finally: CompleteSession() + Release 执行门
```

异常路径：`catch` 中输出 Error 日志后 `throw`（保持失败可观测，由调用方捕获）。

#### BuildExecutionBatches（Kahn 拓扑排序）

```text
① 初始化 indegree（入度表）/ adjacency（邻接表），按连接累加入度
② 队列装载所有入度=0 的节点（按名称排序保证确定性）
③ 循环：出队本层全部节点为一批 batch
     对每个出队节点：visitedCount++，把邻接目标入度-1，归零者入队
④ 若 visitedCount != 节点总数 → 抛 "Workflow contains a cycle" 异常
```

返回 `IReadOnlyList<IReadOnlyList<NodeData>>`——**同批节点相互独立，可并发执行**。
`BuildExecutionPlan` 只是批次拍平后的便利 API。

#### ExecuteNodeAsync（单节点执行）

```text
startedAt = UtcNow
inputSnapshot = 输入端口值快照
if node.IsBreakpointEnabled:
    ① node.SetStatus(Paused)
    ② await _debugController.PauseAtBreakpointAsync(node, ct)   // 阻塞直至 Resume
node.SetStatus(Running) + Info 日志
nodeType = _nodeManager.GetNodeType(node.NodeTypeId)
result = await nodeType.ExecuteAsync(request, context, ct)      // 请求携带 inputSnapshot
ApplyNodeResult(node, result, context)   // 写输出端口 + 全局变量 + 端口快照 + 事件
PropagateOutputs(workflow, node, context) // 沿出线把源端口值写到目标输入端口
node.SetStatus(Success) + 消息/错误清空
CaptureExecutionArtifacts(...)           // 追加 NodeExecutionRecord + NodeExecutionSnapshot
catch: node.SetStatus(Failed) + MarkPortsFailed(所有端口) + 记录错误 + 抛异常
```

`ApplyNodeResult` 细节：遍历 `node.OutputPorts`，从 `result.OutputValues` 取对应端口 Id 的值，`SetValue` 后 `context.CapturePortValue` + `RaisePortValueChanged`；再遍历 `result.GlobalVariables` 写入 `context.GlobalVariables`。

`PropagateOutputs` 细节：对每条出线 `connection`，取源端口值写入目标端口（`SetValue` + 端口快照 + 事件）。**数据传递发生在节点成功执行后**，下游节点所在批次晚于本批次，天然满足依赖。

### 4.7 节点注册中心（NodeManager / NodeFactory）

#### NodeManager

私有字段：`_nodes`(Dictionary\<string,NodeData>，当前工作流节点索引)、`_nodeTypes`(Dictionary\<string,IWorkflowNodeDefinition>，类型注册表)，均忽略大小写。

| 方法 | 说明 |
| --- | --- |
| `RegisterType(IWorkflowNodeDefinition)` | 注册类型；NodeTypeId 重复抛异常 |
| `AttachWorkflow(WorkflowData)` | 清空 `_nodes` 后重建索引 |
| `GetNode(nodeId)` | 未找到抛 `KeyNotFoundException` |
| `GetNodeType(nodeTypeId)` | 类型查询 |
| `GetNodeDescriptors()` | 按 Category 再按 DisplayName 排序返回全部描述 |
| `GetPort(nodeId, portId)` | 经 `FindPort` 查找 |
| `GetIncomingConnections(workflow, nodeId)` / `GetOutgoingConnections` | 按目标/源节点过滤连接 |

#### NodeFactory

`CreateNode(nodeTypeId, x, y, nodeId?)`：`nodeId` 为空时自动生成 `"node-{Guid:N}"`，否则用指定 Id；最终委托 `definition.CreateNode`。`GetAvailableNodes()` 透传 `NodeManager.GetNodeDescriptors()`。

### 4.8 事件总线（WorkflowEventHub）

[WorkflowEventHub.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/WorkflowEventHub.cs)

极简中介者：两个事件 `NodeStatusChanged`、`PortValueChanged` 与对应 `Publish*` 方法。引擎在状态变更时发布，Workbench VM 订阅后经 Dispatcher 刷新 UI。

### 4.9 连接校验器（WorkflowConnectionValidator）

[WorkflowConnectionValidator.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/WorkflowConnectionValidator.cs)

接口 `IWorkflowConnectionValidator` 三个方法：`ValidateConnection`（节点/端口重载 + ConnectionData 重载）、`EnsureWorkflowIsValid`。

校验规则（按序短路，返回 `ConnectionValidationResult`）：

| # | 规则 | 失败消息 |
| --- | --- | --- |
| 1 | 源端口 ≠ 目标端口 | 同一端口不能同时作为起点和终点 |
| 2 | 源节点 ≠ 目标节点 | 暂不支持节点连接到自身 |
| 3 | 方向 = Output→Input | 仅支持输出端口连接输入端口 |
| 4 | CLR 类型 + 语义类型兼容 | 端口类型不兼容（带显示名） |
| 5 | 排除 `ignoredConnection` 后不重复 | 相同的连线已存在 |
| 6 | 目标输入端口单入线 | 输入端口仅允许保留一条入线 |

`EnsureWorkflowIsValid`：先 `EnsureUniqueNodeIds`（按 Id 分组查重），再逐连接校验（`ignoredConnection` 传自身以允许编辑场景）。`ConnectionValidationResult` 提供 `Success()`/`Failure(message)` 静态工厂。

### 4.10 插件管理器（PluginManager）

[PluginManager.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/PluginManager.cs)

私有字段：`_plugins`(Dictionary\<string,IWorkflowPlugin>，忽略大小写)、`_initialized`(bool，幂等标记)。

- `Register(plugin)`：重复 Id 抛异常。
- `InitializeAsync(ct)`：首次调用时对所有插件执行 `InitializeAsync` 并置位。
- `DisposeAsync()`：逐个 `DisposeAsync`，清空并复位；`Dispose()` 同步阻塞包装（供 Autofac 释放）。

### 4.11 调试控制器（WorkflowDebugController）

[WorkflowDebugController.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/WorkflowDebugController.cs)

接口事件：`LogEmitted`(WorkflowLogEventArgs)、`BreakpointHit`(BreakpointHitEventArgs)；属性 `IsPaused`；方法 `StartSession`/`CompleteSession`/`EmitLog`/`PauseAtBreakpointAsync`/`Resume`。

核心字段：`_resumeSignal`（TaskCompletionSource\<bool>，`RunContinuationsAsynchronously`）。

断点暂停/恢复机制：

```text
PauseAtBreakpointAsync(node, ct):
  lock 内创建新的 TCS 存入 _resumeSignal
  BreakpointHit?.Invoke(...)          # 通知 UI 进入暂停态
  ct.Register(() => resumeSignal.TrySetCanceled(ct))   # 取消联动
  await resumeSignal.Task             # 阻塞直至 Resume() 或取消
  lock 内若仍引用本 TCS 则清空
Resume():
  lock 内取出 TCS 并清空 _resumeSignal
  resumeSignal?.TrySetResult(true)    # 释放等待中的节点
```

事件参数：`WorkflowLogEventArgs.Entry`；`BreakpointHitEventArgs.NodeId/NodeName`。

### 4.12 持久化服务（WorkflowPersistenceService）

[WorkflowPersistenceService.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Core/Services/WorkflowPersistenceService.cs)

JSON 序列化选项：`PropertyNamingPolicy = CamelCase` + `WriteIndented`。文件扩展名建议 `.workflow.json`（由 Desktop 的文件对话框约定）。

#### 序列化 DTO（私有嵌套类）

| DTO | 字段 | 说明 |
| --- | --- | --- |
| `WorkflowDocument` | `Version`(=1)、`Nodes`、`Connections`、`GlobalVariables` | 根文档 |
| `WorkflowNodeDocument` | `NodeId`、`NodeTypeId`、`X`、`Y`、`IsBreakpointEnabled`、`Settings`(Dictionary\<string,WorkflowValueDocument>) | 节点持久化形态 |
| `WorkflowConnectionDocument` | Source/Target 四件套 | 连接 |
| `WorkflowValueDocument` | `TypeName`(AssemblyQualifiedName)、`Value`(JsonElement) | 通用值包装 |

#### SaveAsync

```text
Directory.CreateDirectory(父目录)
CreateDocument(workflow)   # 节点→NodeDocument（Settings 反射快照），连接→ConnectionDocument，
                           # 全局变量→ValueDocument（值类型动态决议）
EnsureWorkflowIsValid      # 保存前全图校验
JsonSerializer.SerializeAsync
```

`CreateSettingsDocument` 使用 `GetPersistedProperties`：仅序列化**可读可写、无索引参数、且非 Title/Description** 的公共属性（Title/Description 由 Descriptor 重建，不入文件）。

#### LoadAsync

```text
Deserialize<WorkflowDocument>
RestoreWorkflow:
  ① Version != 1 → 抛异常
  ② 遍历 NodeDocument → _nodeFactory.CreateNode(typeId, x, y, nodeId) 重建节点
  ③ ApplySettings（按属性名匹配反序列化回写）
  ④ 恢复断点标记
  ⑤ 重建连接（校验源/目标端口存在）
  ⑥ 恢复全局变量（DeserializeValue）
  ⑦ EnsureWorkflowIsValid
```

`CreateValueDocument`：`TypeName` 优先取值的实际类型，否则声明类型；值经 `JsonSerializer.SerializeToElement`。
`DeserializeValue`：先 `Type.GetType(TypeName)` 解析类型（失败回退到 `fallbackType`），类型可用则强类型反序列化；否则按 `JsonValueKind` 降级为 null/string/bool/int64/decimal/double/原始克隆。

---

## 5. Workbench 层（工作台 UI）

### 5.1 ViewModel 体系

#### WorkflowWorkbenchViewModel（工作台中枢）

文件：[WorkflowWorkbenchViewModel.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Workbench/ViewModels/WorkflowWorkbenchViewModel.cs)

私有字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `ViewportPadding` / `EstimatedNodeWidth` / `EstimatedNodeHeight` | double 常量 | 视图适配估算参数（120 / 220 / 164） |
| `_workflowEngine` 等 7 个服务 | — | 构造注入 |
| `_executionGate` | SemaphoreSlim(1,1) | 工作台侧执行互斥 |
| `_nodeIndex` | Dictionary\<string,NodeViewModel> | NodeViewModel 快速索引 |
| `_workflow` | WorkflowData | 当前编辑的工作流图 |

ObservableProperty（CommunityToolkit 源生成）：

| 属性 | 类型 | 联动 |
| --- | --- | --- |
| `StatusMessage` | string = "就绪" | 全局状态栏 |
| `GlobalVariablesText` | string = "尚未执行" | 左侧面板文本 |
| `IsBusy` | bool | 联动 `WorkflowStateText`、刷新全部命令可用性 |
| `IsDocumentBusy` | bool | 文档操作中标志 |
| `CurrentWorkflowFilePath` | string? | 联动 `WorkflowDocumentText` |
| `ViewportLocation` / `ViewportSize` / `ViewportZoom` | Point/Size/double | 画布视口（与 NodifyEditor 双向绑定），联动 `ViewportText` |
| `SelectedNode` / `SelectedConnection` | NodeViewModel?/ConnectionViewModel? | 单选 |
| `IsExecutionPaused` / `PausedNodeName` | bool / string? | 断点暂停态，联动 `DebugStateText`、`ResumeExecutionCommand` |

集合属性：`Nodes`、`Connections`、`SelectedNodes`、`SelectedConnections`、`AvailableNodes`(节点库)、`ExecutionLogs`。

命令（21 个）：`ExecuteWorkflowCommand`(Async)、`SaveWorkflowCommand`(Async)、`LoadWorkflowCommand`(Async)、`ResetWorkflowCommand`、`DeleteSelectionCommand`、`RemoveNodeCommand`、`RemoveConnectionCommand`、`DisconnectConnectorCommand`、`AddNodeCommand`、`ToggleThemeCommand`、`ResumeExecutionCommand`、`ToggleBreakpointCommand`、`FitGraphCommand`、`FitSelectionCommand`、`ResetViewportCommand`、`SelectAllNodesCommand`、`ClearExecutionLogCommand`。

派生展示属性：`WorkflowStateText`("执行中/处理中/就绪")、`WorkflowDocumentText`、`WorkflowGraphSummary`、`CurrentThemeText`、`MinViewportZoom`(0.2)/`MaxViewportZoom`(2.5)、`ViewportText`、`SelectionText`、`DebugStateText`、`ExecutionLogSummary`。

关键方法逻辑：

| 方法 | 逻辑 |
| --- | --- |
| `AddNode(item, location)` | 引擎侧 `_nodeFactory.CreateNode` → `_workflow.AddNode` → 建 `NodeViewModel` 入 `Nodes` + 索引 → 刷新视觉状态 |
| `Connect(source, target)` | 忙碌时拒绝；内部调 `TryAddConnection` |
| `TryAddConnection` | 校验 → `_workflow.Connect` → 建 `ConnectionViewModel` → 端口 `AttachConnection` |
| `RewireConnection(currentTarget, newTarget)` | 仅支持输入端口重定向；遍历原目标入线，逐条删除重建到新目标；成功数>0 才生效 |
| `ExecuteWorkflowAsync` | `_executionGate.WaitAsync(0)` → 清日志/高亮 → `Task.Run(() => _workflowEngine.ExecuteAsync(_workflow))`（**卸载到线程池，避免同步节点阻塞 UI**）→ 刷新状态 + 全局变量文本 |
| `SaveWorkflowAsync` / `LoadWorkflowAsync` | 文档选择器 → 持久化服务 → 更新 `_workflow` → `RebuildViewModels` |
| `ResetWorkflow` | 清空节点/端口运行态、日志、暂停态 |
| `ToggleBreakpoint` | 反转选中节点断点并刷新 |
| `FitGraphToViewport` / `FitSelectionToViewport` | `ApplyViewportToBounds`：节点包围盒 + Padding → 计算缩放（clamp 到 0.2~2.5）→ 居中视口 |
| `DeleteSelection` / `RemoveNodeInternal` / `RemoveConnectionInternal` | 同步删除模型层 + VM 层集合，级联清理端口连接 |
| `RebuildViewModels` | 从 `_workflow` 全量重建 Nodes/Connections/选中态/索引 |
| `RefreshGraphVisualState` | `ApplyConnectionState`（按连接把端口 MarkConnected，无数据端口 Clear）+ 全部节点 `Refresh()` |
| 事件订阅处理 | `OnNodeStatusChanged`/`OnPortValueChanged`/`OnLogEmitted`/`OnBreakpointHit` 均先 `Dispatcher.CheckAccess()`，非 UI 线程则 `InvokeAsync` 封送；日志插入头部并限长 200 条 |
| `CreateDemoWorkflow` | 构造内置 Demo：`输入节点 → 转换节点 → 预览节点`（三个固定节点 Id），并预置 `GlobalVariables["GlobalVariablesSeedText"]="DevWorkflow Studio"` |

#### NodeViewModel（节点 VM 包装）

字段：`_location`(Point)、`_status`(NodeStatus)。构造时由 `Model` 一次性生成端口 VM、按 `GroupName` 分组（`BuildGroups`，组内按标题排序）。

| 属性 | 说明 |
| --- | --- |
| `Model` | NodeData 模型引用 |
| `InputPorts` / `OutputPorts` / `InputGroups` / `OutputGroups` | 端口与分组集合 |
| `Id` / `Title` / `Category` / `Description` | 元数据透传 |
| `NodeTypeText` / `DebugStateText` | 节点类型 / 断点状态文本 |
| `SettingsCount` / `SettingsSummary` / `HasEditableSettings` | 反射枚举设置项（排除 Title/Description）用于悬浮提示 |
| `Location` | 双向同步 `Model.Layout.X/Y` |
| `Status` / `StatusText` / `HasError` / `ErrorText` / `LastMessageText` | 运行状态与错误展示 |
| `PortSummary` / `PreviewText` | 端口统计 / 首个非空端口值预览 |

方法：`FindPort`、`AttachConnection`(转发给源/目标端口)、`Refresh`(从模型刷新全部)、`NotifySettingsChanged`、`SetBreakpointEnabled`、`BuildGroups`/`GetSettingsEntries`/`BuildSettingsSummary`/`FormatSettingValue`。

#### PortViewModel（端口 VM 包装）

字段：`_anchor`(Point，连接线锚点)、`_value`、`_status`。`Connections` 为 ObservableCollection。

| 属性 | 说明 |
| --- | --- |
| `Owner` / `Model` / `Id` / `Title` / `GroupName` | 归属节点 + 模型 + 元数据 |
| `DataTypeName` / `SemanticTypeKey` / `SemanticBadgeText` / `TypeDisplayText` | 类型展示（空语义显示"未指定"） |
| `SemanticAccentBrush` / `SemanticBadgeBackground` / `SemanticBadgeForeground` | **按语义类型着色**（TEXT/CSV/TSV/PATH/PREVIEW/IMG/GEN 各自配色，`CreateBrush` 冻结画刷） |
| `Direction` / `Anchor` / `Value` / `Status` | 方向 + 可观察值/状态（私有 set） |
| `IsCollapsed` / `IsConnected` / `ConnectionCount` | 折叠/已连接(=Connected\|HasData)/连接数 |
| `StatusText` / `DisplayValue` / `ToolTipText` | 文本展示 |

方法：`Refresh`(模型→VM 同步)、`AttachConnection`/`DetachConnection`、`SetCollapsed`(写回 `Model.IsCollapsed`)。

#### PortGroupViewModel（端口分组）

`Name`、`Ports`(ObservableCollection)、`IsExpanded`(ObservableProperty，默认 true)、`PortCount`、`Summary`。
`OnIsExpandedChanged`：展开状态同步到组内所有端口 `SetCollapsed(!value)`，并刷新 Summary——**实现 UI 端口折叠/展开**。

#### 其它 VM

| VM | 说明 |
| --- | --- |
| `ConnectionViewModel` | 包装 `ConnectionData` + `Source`/`Target`(PortViewModel)；`DisplayName` = "源.端口 → 目标.端口" |
| `PendingConnectionViewModel` | `StartCommand`(记录源端口) / `FinishCommand`(调 `workbench.Connect`) 驱动连线 |
| `NodeLibraryItemViewModel` | 节点库条目，透传 `NodeDescriptor` |
| `ExecutionLogItemViewModel` | 日志行：`TimestampText`("HH:mm:ss.fff")、`LevelText`(大写)、`NodeText`(空→"系统")、`AccentBrush`(级别配色) |
| `NodeParameterEditorViewModel` | 设置编辑：构造时 `CloneSettings`（反射拷贝副本，隔离编辑）；`CreateSettingsView` 用 `SettingsViewType` 反射创建视图并绑定副本；`TryApplyChanges` 经 `CopyWritableProperties` 回写模型 |

### 5.2 View 与交互

#### WorkflowWorkbenchView（[xaml](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Workbench/Views/WorkflowWorkbenchView.xaml) / [cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Workbench/Views/WorkflowWorkbenchView.xaml.cs)）

布局：顶部工具栏 + 左侧边栏（节点库 + Global Variables，可拖分隔条）+ 右侧画布区（NodifyEditor + Minimap + 图例浮层 + 选中面板）+ 底部执行日志。

| 功能 | 实现要点 |
| --- | --- |
| 节点库拖拽建节点 | `LibraryNode_PreviewMouse*` 记录拖拽起点 → `DragDrop.DoDragDrop` 携带 `NodeLibraryItemViewModel` → `Editor_Drop` 用 `editor.GetLocationInsideEditor` + `SnapToGrid` 落点 → `workbench.AddNode(item, point)` |
| 端口连线 | `PendingConnectionTemplate` 绑定 `PendingConnectionViewModel` 的 Start/Finish 命令；`NodeInput/NodeOutput` 模板绑定 `Anchor`(OneWayToSource)、`IsConnected`、语义色边框 |
| 连线重定向 | 主题样式桥接 + `WorkflowWorkbenchConnectorInteractions`（见第 10 章） |
| 双击编辑设置 | `Node_MouseDoubleClick` 打开 `NodeParameterEditorWindow`，`ShowDialog()==true` 时回调 `NotifyNodeSettingsChanged` |
| 快捷键 | Editor.InputBindings：Del 删除、Ctrl+A 全选、Ctrl+B 断点、Ctrl+F 全图、Ctrl+Shift+F 适配选中、Ctrl+0 重置缩放、F5 继续 |
| 执行日志自动滚动 | 静态构造 `WorkflowWorkbenchConnectorInteractions.Register()`；View 订阅 `ExecutionLogs.CollectionChanged`，新增时 `Dispatcher.BeginInvoke(Background)` `ScrollToTop` |
| 数据模板 | `CanvasNodeContentTemplate`（节点字形/状态指示灯，Category 决定 IN/FX/OUT 与配色）、`CanvasNodeToolTipTemplate`（完整信息卡片）、`WorkbenchMinimapNodeTemplate`（按 Category 着色） |
| 样式 | 本地样式均 `BasedOn` 主题键（如 `WorkflowWorkbench.ItemContainerStyle`），**不直接基于第三方类型键**（遵循 nodify.md 规则） |

#### NodeParameterEditorWindow

`EditorHost`(ContentControl) 承载反射创建的类型化设置视图；无设置时显示占位提示；"保存"走 `TryApplyChanges` 失败弹框。

---

## 6. Desktop 层（桌面宿主）

| 文件 | 内容 |
| --- | --- |
| [App.xaml.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/App.xaml.cs) | Autofac 容器、启动主窗口、退出释放（见第 3 章） |
| [ApplicationModule.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/ApplicationModule.cs) | 全部 DI 注册（见 3.2） |
| [MainWindow.xaml](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/MainWindow.xaml) | 壳层：直接承载 `WorkflowWorkbenchView`，`DataContext="{Binding Workbench}"` |
| [MainWindowViewModel.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/ViewModels/MainWindowViewModel.cs) | `Title`="Workflow Studio" + `Workbench`(WorkflowWorkbenchViewModel) |
| [WorkflowDocumentPickerService.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/Services/WorkflowDocumentPickerService.cs) | 实现 `IWorkflowDocumentPickerService`：`OpenFileDialog`/`SaveFileDialog`，过滤器 `*.workflow.json`，支持记忆当前目录/文件名 |
| [Views/MainView.xaml](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Desktop/Views/MainView.xaml) | 未参与启动流程的历史遗留视图 |

---

## 7. Nodes 层（内置节点）

所有节点继承 `WorkflowNodeDefinition<TSettings>`，统一按"描述 → 端口 → 执行"三段式实现。下表汇总每个节点：

| 节点（TypeId） | 设置类型（视图） | 输入端口 | 输出端口 | 执行逻辑 |
| --- | --- | --- | --- | --- |
| 输入节点 `demo.node.text-source` | `TextSourceNodeSettingsViewModel.Text`(默认 "Build workflow apps with WPF")（有视图） | — | `content` string/plain | 设置文本为空则回退读取 `GlobalVariables["GlobalVariablesSeedText"]`；`Task.Delay(5000)` 模拟耗时 |
| CSV读取节点 `demo.node.csv-read` | `CsvReadNodeSettingsViewModel.FilePath`(默认 "sample-data.csv")（有视图） | — | `csv-content` string/csv、`source-path` string/path | `File.ReadAllTextAsync` 后输出内容与路径 |
| 转换节点 `demo.node.uppercase-transform` | `EmptyNodeSettings` | `incoming` string/plain | `result` string/plain | 调用 `ITextTransformPlugin.TransformToUppercaseAsync`；Delay 2000；写 `GlobalVariables["LastTransform"]` |
| CSV转TSV节点 `demo.node.csv-to-tsv-transform` | `EmptyNodeSettings` | `incoming` string/csv | `result` string/tsv | 内置 `CsvToTsvConverter`（手写 CSV 解析：引号/双引号转义/CRLF 处理，未闭合引号抛 FormatException） |
| 预览节点 `demo.node.preview` | `EmptyNodeSettings` | `incoming` string/plain | `preview-text` string/preview | 调用 `IPreviewPlugin.BuildPreviewAsync`；Delay 2000；写 `GlobalVariables["LastPreview"]` |
| TSV保存节点 `demo.node.tsv-save` | `TsvSaveNodeSettingsViewModel.FilePath`(默认 "output-data.tsv")（有视图） | `incoming` string/tsv | `saved-path` string/path | 校验路径 → 建目录 → 写文件；写 `GlobalVariables["LastTsvSavedPath"]` |
| 图片读取节点 `opencv.node.image-read` | `OpenCvImageReadNodeSettingsViewModel`（FilePath 默认 "sample-image.png"、ReadMode 默认 Color；有视图） | — | `image` WorkflowImageFrame/image、`source-path` string/path | 调 `IOpenCvImageCodecPlugin.LoadAsync`；写 `GlobalVariables["LastImageSourcePath"]` |
| 灰度化节点 `opencv.node.grayscale` | `EmptyNodeSettings` | `image` image-frame | `result` image-frame | 调 `IOpenCvImageProcessingPlugin.ConvertToGrayscaleAsync` |
| 二值化节点 `opencv.node.threshold` | `OpenCvThresholdNodeSettingsViewModel`（ThresholdValue=127、MaxValue=255、Mode=Binary、AutoConvertToGrayscale=true；有视图） | `image` image-frame | `result` image-frame | 调 `ApplyThresholdAsync`，输出消息含阈值与模式 |
| 图片保存节点 `opencv.node.image-save` | `OpenCvImageSaveNodeSettingsViewModel.FilePath`(默认 "output-image.png")（有视图） | `image` image-frame | `saved-path` string/path | 调 `IOpenCvImageCodecPlugin.SaveAsync`；写 `GlobalVariables["LastImageSavedPath"]` |

设置 VM 均实现 `INodeSettings` 且用 `[ObservableProperty]` 源生成；带 `Available*Modes` 只读集合属性供下拉绑定（如 `AvailableThresholdModes`、`AvailableReadModes`）。设置视图为各节点目录下 `View/` 的 UserControl，直接绑定设置 VM 属性（如 TextSourceNodeSettingsView 绑定 `Text`）。

> 共同点：节点内 `ResolveRequiredPath` 统一校验空路径并 `Path.GetFullPath`；所有文件型节点执行时自动创建父目录。

---

## 8. Plugins 层（内置插件）

文件：[DemoWorkflowPlugins.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Plugins/BuiltIn/DemoWorkflowPlugins.cs) / [OpenCvWorkflowPlugins.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Plugins/BuiltIn/OpenCvWorkflowPlugins.cs)

| 插件 | 实现接口 | 元数据（Id/能力） | 实现说明 |
| --- | --- | --- | --- |
| `UppercaseTransformPlugin` | `IWorkflowPlugin`+`ITextTransformPlugin` | `demo.uppercase-transform` / TextTransform | `TransformToUppercaseAsync` = `ToUpperInvariant()` |
| `PreviewPlugin` | `IWorkflowPlugin`+`IPreviewPlugin` | `demo.preview` / Preview | `BuildPreviewAsync` = 原样返回 |
| `OpenCvImageCodecPlugin` | `IWorkflowPlugin`+`IOpenCvImageCodecPlugin` | `opencv.image-codec` / OpenCvImageCodec | Load：校验文件存在 → `Cv2.ImRead(MapReadMode)` → `CreateFrame` 编码为字节流；Save：`DecodeFrame` → `Cv2.ImWrite`（失败抛异常） |
| `OpenCvImageProcessingPlugin` | `IWorkflowPlugin`+`IOpenCvImageProcessingPlugin` | `opencv.image-processing` / OpenCvImageProcessing | 灰度化按通道数分支（1 克隆 / 3 BGR2GRAY / 4 BGRA2GRAY）；二值化支持 AutoConvertToGrayscale 且强制单通道校验 |

内部助手 `OpenCvImageFrameConverter`：

| 方法 | 逻辑 |
| --- | --- |
| `CreateFrame(Mat, format, sourcePath)` | `Cv2.ImEncode` 编码 → 组装 `WorkflowImageFrame`（含 Width/Height/Channels/IsGrayscale） |
| `DecodeFrame(WorkflowImageFrame)` | `Cv2.ImDecode(Content, Unchanged)`，空内容抛异常 |
| `MapReadMode` / `MapThresholdMode` | 枚举 → OpenCvSharp 枚举映射（含越界抛参） |
| `NormalizeFormat` | 补 `.` 前缀并小写，默认 `.png` |

> 图像帧采用"编码字节流"而非裸 `Mat` 传递，规避跨线程/跨节点共享 `Mat` 的生命周期与释放问题；每次处理时解码、使用后 `using` 释放。

---

## 9. Theme 层（主题系统）

### WorkbenchThemeManager（[WorkbenchThemeManager.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Theme/WorkbenchThemeManager.cs)）

- 枚举 `WorkbenchThemeKind { Light, Dark }`；`ActiveTheme`(默认 Light)、`ActiveThemeDisplayName`。
- `ThemeUris` 字典映射主题到 pack URI；`LoadedThemes` 缓存已加载字典；事件 `ThemeChanged`。
- `EnsureInitialized()`：扫描 `Application.Current.Resources.MergedDictionaries` 中是否已有主题字典（同 URI 匹配），有则同步 `ActiveTheme`，无则 `ApplyTheme(Light)`。
- `SetTheme`/`SetNextTheme`：移除现有主题字典 → 从缓存取（无则按 URI 加载）→ 追加 → 更新 `ActiveTheme` → 触发 `ThemeChanged`。

### 主题资源

| 文件 | 内容 |
| --- | --- |
| `Light.xaml` / `Dark.xaml` | 对称的 `WorkflowWorkbench.*Color` + `*Brush` 资源对（PageBackground、PanelBackground、PanelBorder、SubtleBackground、CanvasOverlay、Accent、SecondaryAccent、Success、Warning、Error、Grid、PortInput、PortOutput、NodeIndicatorMuted、Text、SubtleText、Caption 等），**相同资源键不同色值**，切换时整体替换 |
| `WorkflowWorkbench.xaml` | Nodify 样式桥接层（见下）+ 网格 Geometry/DrawingBrush + `WorkflowWorkbench.*` 卡片/文字样式 |
| `Icons.xaml` | `ThemeIcon`（主题切换按钮图标） |

### Nodify 桥接样式（关键架构约束）

[WorkflowWorkbench.xaml](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Theme/Themes/WorkflowWorkbench.xaml) 完成"第三方类型键 → 包装控件类型键"的映射：

```text
WorkflowWorkbench.EditorStyle          BasedOn {x:Type nodify:NodifyEditor}       → studioNodify:NodifyEditor
WorkflowWorkbench.ItemContainerStyle   BasedOn {x:Type nodify:ItemContainer}      → studioNodify:ItemContainer
WorkflowWorkbench.NodeStyle            BasedOn {x:Type nodify:Node}               → studioNodify:Node
WorkflowWorkbench.NodeInputStyle       BasedOn {x:Type nodify:NodeInput}          → studioNodify:NodeInput
WorkflowWorkbench.NodeOutputStyle      BasedOn {x:Type nodify:NodeOutput}         → studioNodify:NodeOutput
WorkflowWorkbench.LineConnectionStyle  BasedOn {x:Type nodify:LineConnection}     → studioNodify:LineConnection
WorkflowWorkbench.PendingConnectionStyle BasedOn {x:Type nodify:PendingConnection} → studioNodify:PendingConnection
WorkflowWorkbench.MinimapItemStyle     BasedOn {x:Type nodify:MinimapItem}        → studioNodify:MinimapItem
```

业务视图（WorkbenchView）中再基于 `WorkflowWorkbench.*` 键二次扩展（如 `WorkbenchItemContainerStyle`），整条链路**无一处直接引用第三方类型键**，符合 `.trae/rules/nodify.md` 约定。

---

## 10. Nodify 封装层

### FacadeControls（[FacadeControls.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Nodify/FacadeControls.cs)）

对 `Nodify 7.3.0` 的包装类型（业务层只依赖本程序集命名空间）：

| 包装类型 | 基类 | 覆写 |
| --- | --- | --- |
| `NodifyEditor` | `global::Nodify.NodifyEditor` | `GetContainerForItemOverride` 返回带 `TranslateTransform` 的 `ItemContainer`；`IsItemItsOwnContainerOverride` |
| `ItemContainer` | `global::Nodify.ItemContainer` | 构造接收 editor |
| `LineConnection` / `PendingConnection` / `Node` / `NodeInput` / `NodeOutput` | 对应 Nodify 类型 | — |
| `Minimap` | `global::Nodify.Minimap` | 容器覆写 |
| `MinimapItem` | `global::Nodify.MinimapItem` | — |

### WorkflowWorkbenchConnectorInteractions（[WorkflowWorkbenchConnectorInteractions.cs](file:///p:/CSharp/WPF/DevWorkflow.Studio/Workflow.Studio/Workflow.Studio.Nodify/WorkflowWorkbenchConnectorInteractions.cs)）

静态注册（幂等，由 `WorkflowWorkbenchView` 静态构造调用）：

1. `ReplaceHandlerFactory<Connecting>` → `CustomConnecting`：`CanBegin = !RetargetConnections.InProgress`（进行重定向时禁止新建连线）。
2. `RegisterHandlerFactory` → `RetargetConnections`（`DragState<Connector>`）：

```text
触发手势：Ctrl + 鼠标左键（IgnoreModifierKeysOnRelease=true）
CanBegin：DataContext 是输入端口（Direction=="Input"）且 IsConnected==true
OnMouseMove：Editor.MouseLocation → FindTargetConnector → 高亮候选（PendingConnection.SetIsOverElement）
            并实时把原目标端口 Anchor 拉到鼠标位置（UpdateConnections，经反射改 Target.Anchor）
OnEnd：若命中目标且非自身 → 反射调用 Editor.DataContext.RewireConnection(source, target)
       （InvokeRewireConnection：GetMethod("RewireConnection", Public|Instance).Invoke）
OnCancel：重置状态
```

> 该扩展经**反射**读写 VM 属性/方法（`Direction`/`IsConnected`/`Connections`/`Target`/`Anchor`/`RewireConnection`），
> 使封装层与 Workbench VM 保持低耦合（无需程序集引用）。

---

## 11. 测试项目

`Workflow.Studio.Core.Tests`（xunit）：

| 测试类 | 覆盖点 |
| --- | --- |
| `WorkflowConnectionValidatorTests` | 语义类型不兼容被拒（CSV→TSV）；输入端口仅允许单入线 |
| `WorkflowEngineDebugTests` | 断点暂停：`ExecuteAsync` 并行运行 → 等待 `BreakpointHit` → 断言 `IsPaused` → `Resume()` → 断言成功与单条 History |
| `WorkflowPersistenceServiceTests` | 保存/加载往返：节点数、断点标记恢复 |
| `OpenCvWorkflowNodeTests` | 端到端图像流水线（read→grayscale→threshold→save）：生成样例图、执行、断言输出文件存在、节点全 Success、全局变量、输出为单通道 |

---

## 12. 核心流程时序

### 12.1 工作流执行

```text
用户点击"执行工作流"
 └─ WorkflowWorkbenchViewModel.ExecuteWorkflowAsync
     ├─ _executionGate.WaitAsync(0)              // VM 级互斥
     ├─ 清日志/清高亮
     └─ Task.Run(() => WorkflowEngine.ExecuteAsync(workflow))
         ├─ 引擎执行门 WaitAsync(0)               // 引擎级互斥
         ├─ AttachWorkflow / EnsureWorkflowIsValid / ResetWorkflowState
         ├─ DebugController.StartSession()
         ├─ PluginManager.InitializeAsync()
         ├─ ExecutionContext(globalVariables)
         ├─ BuildExecutionBatches()               // Kahn 拓扑
         └─ 逐批 Task.WhenAll(ExecuteNodeAsync)
             ├─ (断点) Paused → PauseAtBreakpointAsync → [UI: BreakpointHit → 暂停态 → 用户 Resume → 继续]
             ├─ Running → ExecuteAsync(节点实现) → ApplyNodeResult → PropagateOutputs
             ├─ Success / Failed
             └─ CaptureExecutionArtifacts → EventHub 发布 → VM Dispatcher 刷新
         └─ SyncGlobalVariables → 完成
     └─ VM: RefreshGraphVisualState + GlobalVariablesText
```

### 12.2 连线创建 / 重定向

```text
创建：NodeOutput 拖拽 → PendingConnection 显示预览 → 落点 NodeInput → FinishCommand
      → Workbench.Connect → ValidateConnection（6 条规则）→ WorkflowData.Connect → ConnectionViewModel
重定向：Ctrl+拖动已连接输入端口 → RetargetConnections 开始
      → 实时拖动原连接 Target.Anchor → 落点新输入端口 → 反射调用 RewireConnection
      → 逐条删旧连 / 建新连（再次校验）→ RefreshGraphVisualState
```

### 12.3 保存 / 加载

```text
保存：DocumentPicker(SaveFileDialog) → EnsureWorkflowIsValid → 反射快照设置 → JSON（camelCase）
加载：DocumentPicker(OpenFileDialog) → Deserialize → Version 检查(=1)
      → NodeFactory.CreateNode 逐节点重建 → ApplySettings → 断点恢复 → 连接重建 → 全局变量恢复
      → EnsureWorkflowIsValid → RebuildViewModels
```

---

## 13. 数据流与事件流

### 13.1 端口数据流（执行期）

```text
上游节点 ExecuteAsync
 └─ NodeExecutionResult.OutputValues[portId]
     └─ ApplyNodeResult → node.OutputPorts[portId].SetValue(value)
         └─ PropagateOutputs（沿 connection）
             └─ targetNode.InputPorts[portId].SetValue(sourcePort.Value)
                 └─ 下游批次节点 ExecuteAsync 读取 request.InputValues
```

### 13.2 事件流（引擎 → UI）

```text
WorkflowEngine (发布)                  WorkflowEventHub (中介)          WorkflowWorkbenchViewModel (订阅)
 ├─ NodeStatusChanged ──────────────► PublishNodeStatusChanged ─────► OnNodeStatusChanged → node.Refresh()
 ├─ PortValueChanged ───────────────► PublishPortValueChanged ──────► OnPortValueChanged → port/node.Refresh()
WorkflowDebugController (发布)
 ├─ LogEmitted ─────────────────────────────────────────────────────► OnLogEmitted → 日志头部插入(限200)
 └─ BreakpointHit ─────────────────────────────────────────────────► OnBreakpointHit → 暂停态 UI
```

所有订阅回调经 `Dispatcher.InvokeAsync` 封送到 UI 线程（非 UI 线程自动切换）。

### 13.3 全局变量流

```text
TextSourceNode: 读 GlobalVariablesSeedText
UppercaseTransformNode / PreviewNode / CsvToTsvTransformNode: 写 LastTransform / LastPreview / LastTsvContent
CsvReadNode / TsvSaveNode / OpenCvImageReadNode / OpenCvImageSaveNode: 写 LastCsvSourcePath /
     LastTsvSavedPath / LastImageSourcePath / LastImageSavedPath
引擎结束: SyncGlobalVariables 回写 WorkflowData → UI 展示 GlobalVariablesText
```

---

## 14. 设计模式与架构特性

| 模式 | 落地位置 | 说明 |
| --- | --- | --- |
| 模板方法 | `WorkflowNodeDefinition<TSettings>.CreateNode` / `ExecuteAsync` | 固定"建设置→建节点→绑视图→建端口"骨架，子类仅实现差异点 |
| 策略 / 依赖倒置 | `IWorkflowConnectionValidator`、能力插件接口（`ITextTransformPlugin` 等） | 节点依赖接口而非实现；校验器可替换 |
| 中介者 / 观察者 | `WorkflowEventHub`、`WorkflowDebugController` 事件 + VM 订阅 | 引擎与 UI 解耦，跨线程事件经 Dispatcher 封送 |
| 单例门卫 | `SemaphoreSlim`（VM 层 + 引擎层双层） | 防止并发执行互相污染 |
| 工厂 + 注册表 | `NodeFactory` / `NodeManager` / `PluginManager` | 类型注册、按 Id 实例化 |
| 适配器 / 外观 | `Workflow.Studio.Nodify` FacadeControls + Theme 桥接样式 | 业务视图隔离第三方控件 |
| 组合 | 节点-端口-分组（`PortGroupViewModel`） | 端口分组折叠 |
| 命令 | CommunityToolkit `RelayCommand` / `AsyncRelayCommand`（含 NotifyCanExecuteChanged 联动） | 全部工具栏/快捷键操作 |
| 模型-视图同步 | NodeViewModel/PortViewModel 手动 `Refresh()` | 事件驱动单向刷新 |
| 不可变 DTO | `PortMetadata` / `ConnectionData` 等 init-only | 连接/元数据防篡改 |

架构特性总结：

- **四层纵向切割**：Presentation(Desktop/Workbench Views) → Application(Workbench VM) → Domain+Engine(Core) → Extension(Nodes/Plugins)。
- **横向能力解耦**：节点定义（结构）与能力插件（行为）双层解耦，DI 组装。
- **执行并发**：DAG 同批节点并行、异步执行；`Task.Run` 卸载避免 UI 卡顿。
- **调试能力**：断点暂停（TCS 信号量）+ 执行历史/快照 + 分级日志，UI 实时反馈。
- **持久化自描述**：值携带 AssemblyQualifiedName，支持类型解析与降级还原。
- **主题/控件封装**：稳定资源键 + 明暗切换 + 第三方样式桥接三层隔离。

---

## 15. 已知问题与演进方向

依据 README 与实际代码观察：

1. **可空性警告**：核心模型仍有若干可空性警告（构建不失败）。
2. **持久化局限**：文件格式无版本迁移/兼容策略；`Type.GetType` 依赖程序集可加载性。
3. **插件加载**：仍为启动时手工注册（`ApplicationModule`），无动态 DLL 发现/`AssemblyLoadContext`。
4. **反射耦合**：`WorkflowWorkbenchConnectorInteractions` 依赖字符串反射调用 `RewireConnection`，重命名需同步。
5. **线程模型**：`WorkflowDebugController.Resume` 与引擎等待跨线程，依赖 TCS 的 `RunContinuationsAsynchronously` 保证不阻塞。
6. **UI 测试缺口**：仅有 Core 层测试，缺桌面端集成测试。
7. **遗留文件**：`MainView`（未使用）、`Workbench/Class1.cs`（占位）可清理。
8. **可演进方向**：单步/条件断点、执行取消按钮、最近文件、节点库搜索、`Mat` 直通优化（当前字节编解码有性能开销）、虚拟化大图优化。

---

*文档生成依据：解决方案全部 8 个项目、62 个源文件的静态代码分析，核对至 2026-08-09。*
